---
phase: 01-foundation-authentication
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/services/redis.ts
  - src/routes/auth/verify-email.post.ts
  - src/routes/user/avatar.put.ts
  - vercel.json
  - package.json
  - .env.example
autonomous: false
requirements:
  - R1.8
  - R1.9

must_haves:
  truths:
    - "Redis is configured for session/token blacklist"
    - "Object storage is configured for avatar uploads"
    - "Email verification endpoint exists"
    - "Application can be deployed to Vercel"
    - "All environment variables are documented"
  artifacts:
    - path: "src/services/redis.ts"
      provides: "Redis client configuration"
      exports: ["redis", "blacklistToken", "isTokenBlacklisted"]
    - path: "vercel.json"
      provides: "Vercel deployment configuration"
      contains: ["builds", "routes", "env"]
    - path: ".env.example"
      provides: "Environment variable template"
      contains: ["DATABASE_URL", "JWT_SECRET", "AUTH_SECRET", "REDIS_URL"]
    - path: "src/routes/auth/verify-email.post.ts"
      provides: "Email verification endpoint"
      exports: ["POST /api/auth/verify-email"]
    - path: "src/routes/user/avatar.put.ts"
      provides: "Avatar upload endpoint"
      exports: ["PUT /api/user/avatar"]
  key_links:
    - from: "src/services/redis.ts"
      to: "src/routes/auth/logout.post.ts"
      via: "Token blacklist check"
      pattern: "blacklistToken|isTokenBlacklisted"
    - from: "vercel.json"
      to: "src/index.ts"
      via: "Serverless function entry"
      pattern: "src/index"
---

<objective>
配置 Redis 缓存、对象存储和 Vercel 部署

Purpose: 完成云端基础设施配置，支持生产环境部署
Output: Redis 服务 + 对象存储配置 + Vercel 部署文件
</objective>

<execution_context>
@C:/Users/ex_linhs4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ex_linhs4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/RESEARCH.md
@.planning/phases/01-foundation-authentication/01-01-PLAN.md
@.planning/phases/01-foundation-authentication/01-02-PLAN.md

<interfaces>
<!-- Refresh token interface from Plan 01 -->
From src/db/schema.ts:
```typescript
export interface RefreshToken {
  id: string;
  user_id: string;
  token_hash: string;
  expires_at: Date;
  created_at: Date;
}
```

<!-- Auth middleware from Plan 02 -->
From src/middleware/auth.ts:
```typescript
export function authenticate(req: Request, res: Response, next: NextFunction): void;
export function optionalAuth(req: Request, res: Response, next: NextFunction): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Redis for token blacklist</name>
  <files>src/services/redis.ts, .env.example</files>
  <action>
    1. Add Redis dependencies:
       - npm install @upstash/redis (for Vercel/serverless)
       - or npm install ioredis (for standard Redis)

    2. Create `src/services/redis.ts`:
       - Initialize Redis client with UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN
       - Fallback to local Redis with REDIS_URL if Upstash not configured
       - Export functions:
         - blacklistToken(token, ttl): SET with expiry
         - isTokenBlacklisted(token): GET check
         - deleteKey(key): for cleanup

    3. Update `src/routes/auth/logout.post.ts`:
       - After deleting from database, also blacklist the token in Redis
       - Use TTL matching the token's remaining validity

    4. Update `src/routes/auth/refresh.post.ts`:
       - Check if refresh token is blacklisted before processing
       - Reject blacklisted tokens with 401

    5. Add to `.env.example`:
       - UPSTASH_REDIS_REST_URL=
       - UPSTASH_REDIS_REST_TOKEN=
       - REDIS_URL=redis://localhost:6379
  </action>
  <verify>
    <automated>npm list @upstash/redis | grep @upstash/redis</automated>
    Expected: Package installed
  </verify>
  <done>
    - Redis client configured and connected
    - Token blacklist functions working
    - Logout adds token to blacklist
    - Refresh rejects blacklisted tokens
    - Environment variables documented in .env.example
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement email verification and avatar upload</name>
  <files>src/routes/auth/verify-email.post.ts, src/routes/user/avatar.put.ts</files>
  <action>
    1. Create `src/routes/auth/verify-email.post.ts` with POST /api/auth/verify-email:
       - Accept {email, code}
       - Find verification code in database (purpose='verify_email')
       - Verify code: exists, not used, not expired (5 min)
       - Update user: email_verified = true
       - Mark code as used
       - Create audit log
       - Return 200 OK

    2. Add email verification code sending to registration endpoint:
       - After email registration, generate and store verification code
       - For Phase 1: store code in database (email sending deferred to Phase 3 with SES/SendGrid)
       - For development: log code to console

    3. Create `src/routes/user/avatar.put.ts` with PUT /api/user/avatar:
       - Use authenticate middleware
       - Accept multipart form data with avatar file
       - Use multer for file upload handling
       - Validate: image/*, max 5MB
       - For Phase 1: store as base64 in database or local filesystem
       - Update user record with avatar URL/data
       - Return {avatarUrl}
       - Create audit log

    4. Add to `.env.example`:
       - FILE_UPLOAD_PATH=./uploads (for local storage)
       - MAX_FILE_SIZE=5242880 (5MB in bytes)
  </action>
  <verify>
    <automated>curl -X POST http://localhost:60000/api/auth/verify-email -H "Content-Type: application/json" -d '{"email":"test@example.com","code":"123456"}'</automated>
    Expected: 200 OK or 400/404 for invalid code
  </verify>
  <done>
    - Email verification endpoint exists
    - Verification codes expire after 5 minutes
    - Verified users have email_verified = true
    - Avatar upload accepts image files
    - Avatar stored (locally for Phase 1, cloud in Phase 3)
    - File size and type validation working
  </done>
</task>

<task type="auto">
  <name>Task 3: Configure Vercel deployment</name>
  <files>vercel.json, package.json, .env.example</files>
  <action>
    1. Create `vercel.json`:
       - Configure builds for @vercel/node
       - Set up routes to proxy /api/* to src/index.ts
       - Configure environment variables
       - Set NODE_ENV to production
       - Configure build command: yarn build or tsc

    2. Update `src/index.ts` for serverless compatibility:
       - Export Express app as default export for Vercel
       - Ensure no hardcoded ports (use process.env.PORT)
       - Handle serverless cold starts

    3. Update `package.json`:
       - Add "build": "tsc" script
       - Add "start": "node dist/index.js" script
       - Ensure main points to dist/index.js
       - Add engines.node: ">=23.11.1"

    4. Create `.env.example` with all required variables:
       - DATABASE_URL=postgresql://...
       - JWT_SECRET=
       - AUTH_SECRET=
       - ACCESS_TOKEN_EXPIRY=15m
       - REFRESH_TOKEN_EXPIRY=7d
       - BCRYPT_ROUNDS=12
       - UPSTASH_REDIS_REST_URL=
       - UPSTASH_REDIS_REST_TOKEN=
       - NODE_ENV=development

    5. Create deployment documentation:
       - Steps to deploy to Vercel
       - Steps to create Neon PostgreSQL database
       - Steps to configure environment variables
  </action>
  <verify>
    <automated>yarn build</automated>
    Expected: TypeScript compilation succeeds with no errors
  </verify>
  <done>
    - vercel.json configured correctly
    - TypeScript builds without errors
    - Express app exports for serverless
    - .env.example contains all required variables
    - Deployment documentation exists
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 authentication system with:
    - PostgreSQL database with all tables
    - Email and phone registration
    - Login with JWT tokens
    - Token refresh and logout
    - Password reset flow
    - User profile management
    - Redis token blacklist
    - Vercel deployment configuration
  </what-built>
  <how-to-verify>
    1. **Database check**: Connect to Neon PostgreSQL and verify tables exist:
       - \dt (should show users, refresh_tokens, sms_codes, audit_logs)

    2. **Registration test**:
       ```bash
       curl -X POST http://localhost:60000/api/auth/register/email \
         -H "Content-Type: application/json" \
         -d '{"email":"test@example.com","password":"Test1234","username":"testuser"}'
       ```
       Expected: 201 with userId

    3. **Login test**:
       ```bash
       curl -X POST http://localhost:60000/api/auth/login \
         -H "Content-Type: application/json" \
         -d '{"email":"test@example.com","password":"Test1234"}'
       ```
       Expected: 200 with accessToken and refreshToken

    4. **Protected route test**:
       ```bash
       curl -X GET http://localhost:60000/api/user/profile \
         -H "Authorization: Bearer <accessToken>"
       ```
       Expected: 200 with user profile

    5. **Build check**:
       ```bash
       yarn build
       ```
       Expected: No TypeScript errors
  </how-to-verify>
  <resume-signal>
    Type "approved" if all tests pass, or describe any issues found
  </resume-signal>
</task>

</tasks>

<verification>
1. Redis client connects successfully
2. Token blacklist prevents reused refresh tokens
3. Email verification endpoint validates codes correctly
4. Avatar upload handles image files
5. Vercel configuration is valid
6. TypeScript build completes without errors
7. All environment variables documented
</verification>

<success_criteria>
- Redis operations complete in < 50ms
- Token blacklist effective immediately
- Email verification codes expire after 5 minutes
- Avatar upload accepts files up to 5MB
- Vercel build succeeds
- All environment variables in .env.example
- Deployment documentation complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-03-SUMMARY.md`
</output>
